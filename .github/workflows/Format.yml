name: JuliaFormatter suggestions
on:
  pull_request:
    branches:
      - main
jobs:
  code-style:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: julia-actions/julia-format@v4
        with:
          version: '2.2'
